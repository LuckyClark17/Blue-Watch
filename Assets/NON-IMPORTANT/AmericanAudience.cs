using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmericanAudience : MonoBehaviour
{
    /*//bool
    public bool isGrounded;


    private void OnCollisionStay(Collision collision)
    {
        if (collision.gameObject.CompareTag("AudienceFloor"))
        {
            print("OML");
            isGrounded = true;
        }
    }

    private void OnCollisionExit(Collision other)
    {
        print("BEN");
        isGrounded = false;
    }

    // Update is called once per frame
    void Update()
    {
        
        if (GameManager.Instance.shouldJump && isGrounded)
        {
            print("hi");
            GetComponent<Rigidbody>().AddForce(Vector3.up * GameManager.Instance.AudijumpForce, ForceMode.Impulse);
            print("bye");
        }
    }*/
}
